pipeline {
      agent any 
     stages{
    stage('build') {
       steps{
        sh '''
        echo " Build is succesful on my 'hostname'"
       '''
      }
}

     stage('test') {
steps{
        sh '''
        echo " test is succesful on my 'hostname'"
       '''
      }
}
   stage('deploy') {
steps{
        sh '''
        echo " deploy is succesful on my 'hostname'"
       '''
      }
}
  }
}
