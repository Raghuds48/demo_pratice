pipeline {
       agent any
       stages {
         stage('copy Repositery contents') {
           steps{
      
             sh '''
              mkdir raghu20 && cd raghu20
             git clone --bare https://github.com/Raghuds48/duplicate-repo-history.git
             ls
             cd duplicate-repo-history.git
             ls
             git  push --mirror https://github.com/Raghuds48/wisecow.git
             '''
           }
         }
       }
}
