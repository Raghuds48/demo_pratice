pipeline {
       agent any
       stages {
         stage('copy Repositery contents') {
           steps{
      
             sh '''
              mkdir /root/raghu/checkout git clone --bare https://github.com/Raghuds48/duplicate-repo-history.git
             ls
             cd duplicate-repo-history
             git  push --mirror https://github.com/Raghuds48/wisecow.git
             '''
           }
         }
       }
}
